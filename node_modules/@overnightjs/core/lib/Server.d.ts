import { Application } from 'express';
declare type Controller = InstanceType<any>;
declare type RouterLib = ((options?: any) => any);
export declare class Server {
    private readonly _app;
    private _showLogs;
    private readonly LOG_STR;
    constructor(showLogs?: boolean);
    protected readonly app: Application;
    protected showLogs: boolean;
    protected addControllers(controllers: Controller | Controller[], routerLib?: RouterLib): void;
    private getRouter;
}
export {};
